# Supabase 连接配置
# 复制本文件为 .env（或 .env.development / .env.production）后填入真实值。
# 注意：Taro 只会把以 TARO_APP_ 开头的变量注入小程序运行时，
# 因此变量名带 TARO_APP_ 前缀，代码中通过 process.env.TARO_APP_SUPABASE_* 读取。
# anon key 会被打包进小程序、可被任何人提取，数据安全依赖数据库端的 RLS 策略（见 docs/schema.sql）。

TARO_APP_SUPABASE_URL=
TARO_APP_SUPABASE_ANON_KEY=
